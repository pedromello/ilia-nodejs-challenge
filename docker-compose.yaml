services:

  # Users Microservice ------------
  users-microservice:
    build:
      context: ./users-microservice
      dockerfile: Dockerfile
    ports:
      - "3002:3002"
    env_file:
      - ./users-microservice/.env.development
    depends_on:
      - users-db
    networks:
      - users-microservice
      - shared-network
    volumes:
      - ./users-microservice:/usr/src/app
      - /usr/src/app/node_modules
      - /usr/src/app/src/generated
    command: sh -c "npx prisma generate && npm run start:dev"
  users-db:
    image: postgres:16.0-alpine3.18
    env_file:
      - ./users-microservice/.env.development
    ports:
      - "6432:5432"
    networks:
      - users-microservice
    volumes:
      - ./users-microservice/prisma/migrations:/usr/src/app/prisma/migrations

  # Transactions Microservice ------------
  transactions-microservice:
    build:
      context: ./transactions-microservice
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    env_file:
      - ./transactions-microservice/.env.development
    depends_on:
      - transactions-db
    networks:
      - transactions-microservice
      - shared-network
    volumes:
      - ./transactions-microservice:/usr/src/app
      - /usr/src/app/node_modules
      - /usr/src/app/src/generated
    command: sh -c "npx prisma generate && npm run start:dev"
  transactions-db:
    image: postgres:16.0-alpine3.18
    env_file:
      - ./transactions-microservice/.env.development
    ports:
      - "6431:5432"
    networks:
      - transactions-microservice
    volumes:
      - ./transactions-microservice/prisma/migrations:/usr/src/app/prisma/migrations

networks:
  users-microservice:
    driver: bridge
  transactions-microservice:
    driver: bridge
  shared-network:
    driver: bridge
